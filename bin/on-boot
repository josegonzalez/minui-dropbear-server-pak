#!/bin/sh
bindir="$(dirname "$0")"

main() {
    "$bindir/service-on" &
}

if [ -f "$LOGS_PATH/SSH Server.txt" ]; then
    mv "$LOGS_PATH/SSH Server.txt" "$LOGS_PATH/SSH Server.txt.old"
fi

main "$@" >"$LOGS_PATH/SSH Server.txt" 2>&1
